#include<stdio.h>
void main()
{
    int i,diff;
    int a[5]={1,2,4,7,10};
    diff=a[0];
    for(i=0;i<5;i++)
    {
        if(a[i]-i!=diff)
        {
            while(diff<a[i]-i){
            printf("Missing Element is: %d\n",diff+i);
            diff=diff+1;}
        }
    }
    
}
